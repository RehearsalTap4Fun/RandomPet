Create one 2048×2048 square 2×2 sheet with exactly four unlabelled variants of the SAME modular head-shell prompt. IMAGE ROLES: Image 1 is STYLE REFERENCE ONLY (friendly rounded tactile polished 3D cartoon); do not copy its creature. Image 2 is LOCKED BLOB RIG ALIGNMENT ONLY and must not appear in output. Image 3 is HEAD SLOT MASK/SOCKET GUIDE ONLY; use its position, but do not render guide marks. OUTPUT ONLY `head_shadow_hood`: in each quadrant a compact removable low wide deep-violet hood shell for the top of the head, like a shallow soft cowl cap. It must have one rounded crown, a subtle front fold, a FLAT HORIZONTAL HIDDEN UNDERSIDE/attachment edge, and width greater than height. It must end above the face and cannot extend down into a torso. No full creature, body, torso, belly, neck, face, eye, mouth, limbs, tail, robe, dress, pear silhouette, head contents, backing plate, socket cross, or guide line. Boundary contract: the hood is fully opaque right up to a clean crisp antialiased silhouette; no fuzzy fringe, flyaway fibers, smoke, transparency, semi-transparent veil, glow, cast shadow, or color spill at its edge. Use matte soft-toy shading only inside the shell, with an unblurred outer contour. Same camera/scale/socket for all four, variant only fold nuance. BACKGROUND: perfectly uniform opaque high-saturation pure yellow RGB #FFFF00 on every non-shell pixel and gutter. No gradient, texture, vignette, ground, contact/cast/ambient shadow, reflection, or glow on yellow. Safe quadrant margins. No text, labels, numbers, logos, watermark, gore, or photoreal anatomy.
